name: SnapDrift Enforce
description: Evaluate the SnapDrift summary against diff.mode and fail when required.

inputs:
  summary-path:
    description: Path to the SnapDrift summary JSON.
    required: false
    default: qa-artifacts/snapdrift/drift/current/summary.json

outputs:
  status:
    description: Computed SnapDrift status.
    value: ${{ steps.evaluate.outputs.status }}

runs:
  using: composite
  steps:
    - name: Set action root path
      shell: bash
      run: echo "ACTION_ROOT=$(cd "$GITHUB_ACTION_PATH/../.." && pwd)" >> "$GITHUB_ENV"

    - name: Install shared action dependencies
      shell: bash
      run: npm ci --prefix "$ACTION_ROOT"

    - id: evaluate
      shell: bash
      env:
        SUMMARY_PATH: ${{ inputs.summary-path }}
      run: |
        node --input-type=module <<'EOF'
        import fs from 'node:fs/promises';
        import path from 'node:path';
        import { pathToFileURL } from 'node:url';

        const modulePath = path.resolve(process.env.ACTION_ROOT, 'lib/compare-results.mjs');
        const { shouldFailDriftCheck, formatDriftFailureMessage } = await import(pathToFileURL(modulePath));
        const summary = JSON.parse(await fs.readFile(process.env.SUMMARY_PATH, 'utf8'));

        await fs.appendFile(process.env.GITHUB_OUTPUT, `status=${summary.status || ''}\n`);

        if (shouldFailDriftCheck(summary)) {
          throw new Error(formatDriftFailureMessage(summary.diffMode, summary));
        }
        EOF
